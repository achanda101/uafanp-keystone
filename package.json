{
  "name": "uafanp-keystone-app",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "dev": "keystone dev",
    "start": "keystone start",
    "build": "keystone build && keystone prisma migrate deploy",
    "deploy": "keystone build && keystone prisma migrate deploy && keystone start",
    "migrate": "keystone prisma migrate deploy",
    "db:reset": "keystone prisma migrate reset --force",
    "db:seed": "keystone prisma db seed"
  },
  "dependencies": {
    "@keystone-6/auth": "^8.0.0",
    "@keystone-6/cloudinary": "^8.0.0",
    "@keystone-6/core": "^6.0.0",
    "@keystone-6/fields-document": "^9.0.0",
    "dotenv": "^17.2.0",
    "typescript": "^5.5.0"
  }
}